//controller
'use strict';

xy.controller('QueryMenuController', ['$rootScope', '$scope', '$log', 'PageService', 'StockService','OrderService',
    function($rootScope, $scope, $log, PageService, StockService, OrderService) {

        $log.debug('QueryMenuController');

     	

    }
]);